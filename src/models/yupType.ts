export interface InputValues {
  email: string
  password: string
  firstName: string
  lastName: string
  dateOfBirth: Date // Update the type to Date instead of string
  street: string
  city: string
  zipCode: string
  country: string
}
